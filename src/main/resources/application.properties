spring.application.name=gestor

# Servidor (acessível externamente no VPS)
server.address=0.0.0.0
server.port=8080

# MySQL
spring.datasource.url=jdbc:mysql://187.127.43.161:3306/db_gestor_convetix?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=C0nV3Mu2026
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Jackson - snake_case nos JSONs
spring.jackson.property-naming-strategy=SNAKE_CASE

# Swagger / OpenAPI
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.use-root-path=true
spring.application.swagger.auto-open=true

# JWT
jwt.secret=convertix-gestor-jwt-secret-key-minimo-256-bits-para-hs256-seguro
jwt.timezone=America/Sao_Paulo

# Upload de arquivos
app.upload.dir=uploads
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB
