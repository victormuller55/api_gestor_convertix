# =============================================================================
# Perfil LOCAL — desenvolvimento. Não use em produção.
# =============================================================================

server.address=localhost
server.port=5000

# MySQL local / Docker
spring.datasource.url=jdbc:mysql://187.127.43.161:3306/db_gestor_convertix?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=C0nV3Mu2026
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Swagger liberado apenas no perfil local
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.use-root-path=true
spring.application.swagger.auto-open=true

# JWT (apenas local)
jwt.secret=T#7vQ@2mLp!9Xz$Rk8&fNw3^HyJ1*Bg5%CnUq4=DsE?i6/Za+Mo0]Yx(Wl;Pt

# CORS — localhost de desenvolvimento (inclui a própria API/Swagger na :5000)
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:8080,http://localhost:5000

# Asaas PRODUÇÃO (URL correta: /v3 — sem /api)
asaas.base-url=
asaas.api-key=
asaas.webhook-token=

# Admin padrão (somente perfil local)
app.admin.auto-create=true
app.admin.email=victor@convertix.net.br
app.admin.password=ChangeMe!Local123

# SMTP Hostinger — noreply@convertix.net.br
spring.mail.host=smtp.hostinger.com
spring.mail.port=465
spring.mail.username=noreply@convertix.net.br
spring.mail.password=
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.enable=false
spring.mail.properties.mail.smtp.starttls.required=false
app.mail.from=noreply@convertix.net.br
